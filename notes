to be able to generate tokens
i need to install this agora package
by this command " pip install agora-token-builder "
then use it inside app views

to install crispy form " pip install django-crispy-forms "
then add  'crispy_forms' to installed apps in settings
CRISPY_TEMPLATE_PACK = 'bootstrap4'

connect to pgAdmin -> pip install psycopg2-binary


github command
1. git add .
2. git commit -m "videocall"
3. git push



to kill port process by command " sudo fuser -k 8000/tcp "